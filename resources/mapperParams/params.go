package mapperParams

import (
	"sky-take-out/server/mapper"
)

var EmployeeMapper = new(mapper.EmployeeMapper)
var CategoryMapper = new(mapper.CategoryMapper)
var DishMapper = new(mapper.DishMapper)
var SetmealMapper = new(mapper.SetmealMapper)
var DishFlavorMapper = new(mapper.DishFlavorMapper)
var SetmealDishMapper = new(mapper.SetmealDishMapper)
var UserMapper = new(mapper.UserMapper)
var ShoppingCartMapper = new(mapper.ShoppingCartMapper)
var AddressBookMapper = new(mapper.AddressBookMapper)
var OrderMapper = new(mapper.OrderMapper)
var OrderDetailMapper = new(mapper.OrderDetailMapper)
